package main

import (
    "bufio"
    "fmt"
    "os"
    "strconv"
)

func check(e error) {
    if e != nil {
        panic(e)
    }
}

func main() {
    // Open file and read in one line at a time

        // Figure out how to read in string[space]int from a line

        // Initialize global values and move other logic to a func

    // Output final total
}
